SET(TEST_SOURCES
	test.cc
)

ADD_EXECUTABLE(MookMate_test ${TEST_SOURCES})
INCLUDE_DIRECTORIES(../inc)
ADD_CUSTOM_TARGET(run_test COMMAND ${CMAKE_BINARY_DIR}/test/MookMate_test DEPENDS MookMate_test WORKING_DIRECTORY ${CMAKE_SOURCE_DIR})
